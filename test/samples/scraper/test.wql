SET html TO getRequest WITH path;
SET html TO parse WITH html;
SELECT text FROM html INTO first WHERE SELECTOR IS '#main-tag';
SELECT text FROM html INTO second WHERE SELECTOR IS 'body p';
SET first TO trim WITH first[0].text;
SET second TO trim WITH second[0].text;
SET return TO merge WITH first, second;