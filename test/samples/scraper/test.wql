html = getRequest(path).parse();
first = html.querySelectorAll(s'#main-tag')[].select{ 'text': text(*,), };
second = html.querySelectorAll(s'body > p')[].select{ *.text(), };
first = first[0].text.trim();
second = second[0].text.trim();
return = merge(first, second);