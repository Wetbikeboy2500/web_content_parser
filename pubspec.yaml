name: web_content_parser
description: A parser for online content. This package defines a base design for the realtion of data and parsing of it.
version: 1.0.0

publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  hetu_script: 0.1.2+5
  path: ^1.8.0
  html: ^0.15.0
  http: ^0.13.1
  json_annotation: ^4.0.1
  equatable: ^2.0.0
  convert: ^3.0.0
  intl: ^0.17.0

dev_dependencies:
  puppeteer: ^2.0.0
  pedantic: ^1.11.0
  build_runner: ^2.0.1
  json_serializable: ^5.0.0
  lints: ^1.0.1
  test: ^1.17.10
  coverage: ^1.0.3
  dotenv: ^3.0.0
